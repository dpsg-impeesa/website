name: Update Calendars

on:
  schedule:
    - cron: '0 0 * * *'
  workflow_dispatch:

jobs:
  update-calendar:
    runs-on: ubuntu-latest
    steps:
      - name: Update statichost.eu deployment
        run: curl --request POST https://builder.statichost.eu/dpsg-impeesa
