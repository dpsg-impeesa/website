# Docker image to use for building
image: hubci/hugo:0.145.0
# Build command
command: hugo --gc --minify && public/kalender/get-calendars.sh
# Public directory
public: public
domains:
  # Primary domain
  - dpsg-radolfzell.de
  # Domains that redirect to the primary
  - www.dpsg-radolfzell.de
